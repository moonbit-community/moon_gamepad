import {
}

import {
  "moonbitlang/core/int",
} for "test"

options(
  link: { "native": { "stub-cc-flags": "${build.GAMEPAD_STUB_CC_FLAGS}" } },
  "native-stub": [ "native/backend.c" ],
  targets: {
    "native_backend_native.mbt": [ "native" ],
    "native_backend_stub.mbt": [ "not", "native" ],
  },
)
